#include "main.h"

/**
 * Description: print_alphabet - write alphabet in lowercase
 * Return: (0) is success
 */

void print_alphabet(void)
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++)
{
_putchar(ch);
}
_putchar('\n');
}

