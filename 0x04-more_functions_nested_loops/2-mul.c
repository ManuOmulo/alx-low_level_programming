#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: parameter to be multiplied to
 * @b: parameter to multiply
 * Return: int value of a * b
 */

int mul(int a, int b)
{
int result;
result = a * b;
return (result);
}
